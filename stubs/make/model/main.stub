{{{
  exports({
    to: app.makePath('app/models', entity.path, entity.name + '.ts')
  })
}}}
import { DateTime } from 'luxon'
import { {{ entity.className }}Schema } from '../../database/schema.js'
@each(relationship in relationships)
{{ relationship.importLine }}
@end

export default class {{ entity.className }} extends {{ entity.className }}Schema {
  @each(relationship in relationships)
  {{ relationship.line }}
  @end
}
